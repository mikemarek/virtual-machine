# hello.asm
# Prints the string "Hello World!".
# We can represent bytes as ASCII values or as numerals.

bpush H     bprint
bpush e     bprint
bpush l     bprint
bpush l     bprint
bpush o     bprint
bpush 32    bprint
bpush 87    bprint
bpush 111   bprint
bpush 114   bprint
bpush 108   bprint
bpush 100   bprint
bpush 33    bprint
